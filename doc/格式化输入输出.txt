C语言预处理器
    #define
        #define TAXRATE 0.15
        编译程序时会把所有的TAXRATE都会替换成0.15，此过程称为编译时替换
        建议使用全大写表示符号常量

const 限定符
    const int MONTHS =12;
    限定一个变量为只读，在程序中不可修改

字符串：
    字符串被储存在char类型的数组中，数组由连续的储存单元组成。数组的末尾位置的字符\0，这是空字符，C语言用它标记字符串的结束，它是非打印字符

